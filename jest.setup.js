// código que le permite a node ejecutar el fetchAPI a pesar de que en versiones anteriores a V18 no existe
// Instalar paquete node-fetch o whatwg-fetch
// npm install add -D whatwg-fetch
//Muchas de estas cosas no hay que configurarlas en CreateReactApp porque ya tiene muchas configuradas.

import 'whatwg-fetch'
